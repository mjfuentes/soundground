# Cursor AI Agent Rules for SoundGround

## 🚨 MANDATORY: Task Completion Requirements

**BEFORE MARKING ANY TASK AS COMPLETE, YOU MUST:**

1. **✅ Type Check**: Run `npm run type-check` - must pass with zero errors
2. **🏗️ Build**: Run `npm run build` - must complete successfully  
3. **🔄 RESTART Dev Server**: ALWAYS restart after code changes (Fast Refresh can cause stale code errors)
   - Kill existing: `lsof -ti:3000 | xargs kill -9`
   - Start fresh: `npm run dev` (background)
   - Verify: `sleep 3 && curl -s http://localhost:3000 > /dev/null && echo "✅ Server running"`
4. **🌐 Browser Testing**: Use browser tools to test the changes
   - Navigate to `http://localhost:3000`
   - Take snapshot of the page
   - Test basic navigation and user interactions
   - Verify the specific feature/change works correctly
   - Test error states if applicable
5. **📋 Report**: Provide brief test results summary

**NO EXCEPTIONS.** If any step fails, fix the issue before completing the task.

**⚠️ CRITICAL**: Step 3 (restart) is MANDATORY after ANY code changes. Fast Refresh is unreliable and causes runtime errors. ALWAYS do a clean restart.

---

## Project Context
SoundGround - Sounds from the Underground. A Next.js 15 app for SoundCloud music discovery. Dig deep into artist profiles, explore connections between artists, and uncover hidden gems in the underground music scene. Deployed on Fly.io with caching and enhanced playback features.

## Agent Behavior

### Command Execution Style
For deployment pipelines, slash commands, and automated workflows:
- **ZERO VERBOSE**: Execute silently without commentary
- Report only errors or final status
- No step-by-step narration between commands
- Let command output speak for itself
- Summarize only at the end

## Code Standards

### TypeScript & React
- **Strict type checking, no `any` types**
- **Use centralized types from `/src/types/index.ts`**
- Next.js 15 App Router conventions
- Server components by default, client only when needed (`'use client'`)
- Async/await over promises
- Small, focused components
- Semantic HTML + accessibility attributes
- **Import shared types instead of redefining them**

### Constants & Configuration
- **Use centralized constants from `/src/constants/index.ts`**
- No magic numbers or hardcoded strings
- Examples:
  ```typescript
  import { CACHE_TTL, API_ROUTES, ERROR_MESSAGES } from '@/constants';
  
  const ttl = CACHE_TTL.PROFILE; // ✅ Good
  const ttl = 1800000; // ❌ Bad
  ```

### Logging
- **Use structured logger from `/src/lib/logger.ts`**
- **NEVER use console.log, console.warn, console.error**
- Examples:
  ```typescript
  import { createLogger } from '@/lib/logger';
  
  const logger = createLogger({ component: 'TrackCard' });
  logger.info('Track loaded', { trackId: track.id });
  logger.error('Failed to load', { trackId }, error);
  ```

### Styling
- Tailwind CSS exclusively
- Responsive design (mobile-first)
- Dark mode support where applicable

### Structure
- `/src/app/` - Pages and API routes (App Router)
- `/src/components/` - React components + tests
- `/src/lib/` - Utilities and API clients
- `/src/types/` - Centralized TypeScript types
- `/src/constants/` - App-wide constants
- `/src/test-utils/` - Testing utilities
- `/docs/` - Documentation
- `/scripts/` - Deployment and validation scripts

## Automated Workflows

### Cursor Hooks (Global: `~/.cursor/hooks.json`)
- **After file edit**: `format.sh` - Auto-lint TypeScript/TSX files
- **On agent stop**: `deploy.sh` - Full deployment pipeline

### Deployment Pipeline
Sequential steps (failure stops workflow):
1. **Validation** - Type-check, lint, build, Fly.io checks
2. **Stage** - `git add -A`
3. **Commit & Push** - Semantic commit message
4. **Deploy** - `flyctl deploy --remote-only`
5. **Verify** - Health check with curl

### NPM Scripts
```bash
npm run dev              # Start dev server (port 3000)
npm run build            # Production build
npm run type-check       # TypeScript validation
npm run lint:fix         # Auto-fix linting issues
npm run test             # Run test suite with coverage
npm run test:watch       # Watch mode for development
npm run test:ci          # CI mode with full coverage

npm run deploy           # Full validation + deploy
npm run deploy:check     # Validation only
npm run deploy:fly       # Deploy to Fly.io
npm run validate:env     # Check environment variables
npm run validate:deployment  # Verify deployment health
```

## Slash Commands

Custom commands in `.cursor/commands/` - trigger with `/commandname`

### `/push` - Full Deployment Pipeline
test → commit → push → deploy → verify

Stops on first failure.

## Development Guidelines

### API Routes (`/src/app/api/soundcloud/`)
- **Use structured logger for all logging**
- **Use constants for error messages and status codes**
- Proper error handling with try-catch
- Use HTTP status codes from `@/constants`
- Cache with appropriate TTLs from `@/constants`
- Validate inputs with Zod schemas
- Use TypeScript types from `@/types`
- **Create test file in `__tests__` directory**
- Example:
  ```typescript
  import { createLogger } from '@/lib/logger';
  import { HTTP_STATUS, ERROR_MESSAGES } from '@/constants';
  
  const logger = createLogger({ route: 'profile' });
  
  try {
    // Route logic
  } catch (error) {
    logger.error('Route failed', {}, error as Error);
    return NextResponse.json(
      { error: ERROR_MESSAGES.API.GENERIC },
      { status: HTTP_STATUS.INTERNAL_SERVER_ERROR }
    );
  }
  ```

### Components
- **TypeScript interfaces for props (or use types from `@/types`)**
- **Use structured logger instead of console.log**
- **Use constants for magic numbers and strings**
- Loading and error states
- Next.js Image component for images
- Semantic HTML and ARIA labels
- **Create test file in `__tests__` directory**
- Client components only when necessary (state, events, browser APIs)

### Testing
- **All new code must include tests**
- **Test coverage goal: 70%+ overall, 80%+ for new code**
- Use test utilities from `/src/test-utils/`
- Test file naming: `ComponentName.test.tsx`
- Test structure:
  ```typescript
  import { renderWithProviders, createMockTrack } from '@/test-utils';
  
  describe('ComponentName', () => {
    it('should do something', () => {
      // Arrange, Act, Assert
    });
  });
  ```

### Pre-Deployment Checks
Automated validation ensures:
- ✅ TypeScript compiles
- ✅ ESLint passes
- ✅ Build succeeds
- ✅ Fly.io auth valid
- ✅ Target app exists

## Environment & Configuration

### Environment Variables
- **Development**: `.env.local` (not committed)
- **Production**: Fly.io secrets (`flyctl secrets set KEY=value -a soundground`)
- Auto-detect URL: `process.env.FLY_APP_NAME` or `process.env.VERCEL_URL`

### Development Server
- **Port 3000 ONLY** - no alternatives
- Kill existing: `lsof -ti:3000 | xargs kill -9`
- Start: `npm run dev` → http://localhost:3000

## Quick Reference

### Adding API Endpoint
1. Create `/src/app/api/[endpoint]/route.ts`
2. Create `/src/app/api/[endpoint]/__tests__/route.test.ts`
3. Use types from `/src/types/`
4. Use constants from `/src/constants/`
5. Use structured logger
6. Add error handling with try-catch
7. Add caching if appropriate (use TTLs from constants)
8. Write tests covering success, error, and edge cases

### Adding Component
1. Create `/src/components/ComponentName.tsx`
2. Create `/src/components/__tests__/ComponentName.test.tsx`
3. Use types from `/src/types/` for props
4. Use structured logger if needed
5. Accessibility attributes (ARIA labels)
6. Tailwind styling (mobile-first)
7. Write tests covering rendering, interactions, and states

### Best Practices
- **Use centralized types and constants**
- **Use structured logger (never console.log)**
- **Write tests for all new code**
- Semantic commit messages (conventional commits)
- Test locally before deploying
- Documentation updated with code changes
- Environment variables for secrets
- Proper error boundaries
- Optimize images/assets
- No `any` types

### Code Quality Enforcement
- **Pre-commit hooks** run automatically:
  - Lint-staged (auto-fix)
  - Type check
  - Tests for changed files
- **CI/CD pipeline** enforces:
  - All tests pass
  - Type check passes
  - Build succeeds
  - Code quality checks
- **Coverage requirements:**
  - Overall: 70%+
  - New code: 80%+

## Fly.io Deployment

### Configuration
- **App**: `soundground`
- **Region**: `iad` (US East)
- **Memory**: 1GB
- **Scaling**: Auto (min 0, auto-stop)
- **Deploy**: `flyctl deploy --remote-only`

### Setup
```bash
# Install Fly CLI
curl -L https://fly.io/install.sh | sh

# Authenticate
flyctl auth login

# Set secrets
flyctl secrets set SOUNDCLOUD_CLIENT_ID=your_id -a soundground
```

## Troubleshooting

| Issue | Solution |
|-------|----------|
| Hooks don't trigger | Restart Cursor |
| Validation fails | Run `npm run type-check`, `npm run lint:fix`, `npm run build` individually |
| Deployment fails | Check `flyctl status -a soundground` |
| Linting errors | Run `npm run lint:fix` |
| Check logs | `flyctl logs -n -a soundground` (use `-n` flag to avoid streaming) |
| Port 3000 blocked | `lsof -ti:3000 \| xargs kill -9` |

---

## Documentation

### Available Documentation
- **Quick Reference**: `/docs/REFERENCE.md` - Quick lookup guide
- **API Reference**: `/docs/API.md` - Complete API endpoint documentation
- **Architecture**: `/docs/ARCHITECTURE.md` - System architecture and design decisions
- **README**: `/README.md` - Project overview and setup

### When to Update Documentation
- **New API endpoint**: Update `/docs/API.md`
- **Architecture change**: Update `/docs/ARCHITECTURE.md`
- **User-facing feature**: Update `/README.md`

## Testing Strategy

### Test Types
1. **Unit Tests** - Components and utilities
2. **Integration Tests** - API routes and data flow
3. **End-to-End Tests** - Critical user flows (future)

### Test Coverage Goals
- Components: 80%+
- API Routes: 90%+
- Utilities: 90%+
- Overall: 70%+

### Running Tests
```bash
npm test                # Run all tests with coverage
npm run test:watch      # Watch mode during development
npm run test:ci         # CI mode (used in GitHub Actions)
```

## CI/CD Pipeline

### GitHub Actions Workflows
1. **CI** (`.github/workflows/ci.yml`)
   - Triggers: Push to main, PRs
   - Steps: Lint, type-check, test, build

2. **Deploy** (`.github/workflows/deploy.yml`)
   - Triggers: Push to main
   - Steps: Deploy to Fly.io, health check

3. **PR Checks** (`.github/workflows/pr-checks.yml`)
   - Triggers: Pull requests
   - Steps: Bundle size, security audit, code quality

### Pre-commit Hooks
Located in `.husky/pre-commit`:
- Lint-staged (auto-fix)
- Type check
- Tests for changed files
- **Note**: Build check runs in CI, not locally

## Summary

**Remember**: Follow the mandatory task completion requirements at the top of this file for EVERY task. No shortcuts.

### Key Principles
1. **Type Safety**: Use centralized types from `/src/types/`
2. **No Magic Values**: Use constants from `/src/constants/`
3. **Structured Logging**: Use logger from `/src/lib/logger.ts`
4. **Test Everything**: Write tests for all new code
5. **Document Changes**: Update relevant documentation

