echo "⚡ Running fast pre-commit checks..."

# Run lint-staged (auto-fix)
npx lint-staged

# Type check
echo "📝 Type checking..."
npm run type-check

echo "✅ Pre-commit checks passed!"
echo "💡 Tests, coverage, and build checks will run in CI pipeline"
