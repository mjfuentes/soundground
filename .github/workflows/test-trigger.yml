name: Test Trigger

on:
  push:
    branches: [main]

jobs:
  test:
    name: Test that workflows trigger
    runs-on: ubuntu-latest
    steps:
      - name: Echo test
        run: echo "GitHub Actions is working!"

